{
  "name": "claude-code-logger",
  "version": "1.0.2",
  "description": "HTTP/HTTPS proxy logger for analyzing Claude Code traffic with enhanced chat mode visualization",
  "main": "dist/index.js",
  "bin": {
    "claude-code-logger": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/cli.js",
    "dev": "ts-node src/cli.ts",
    "dev:log": "stdbuf -oL -eL ts-node src/cli.ts 2>&1 | tee middleware-logger.log",
    "dev:log-example": "stdbuf -oL -eL ts-node src/cli.ts start -p 8000 -h api.anthropic.com -r 443 --https --chat-mode --verbose --debug 2>&1 | tee middleware-logger.log",
    "lint": "eslint src --ext .ts",
    "typecheck": "tsc --noEmit"
  },
  "keywords": [
    "claude",
    "claude-code",
    "anthropic",
    "proxy",
    "logger",
    "middleware",
    "logging",
    "http",
    "https",
    "ai",
    "debugging",
    "chat"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/marked-terminal": "^6.1.1",
    "@types/node": "^20.0.0",
    "@typescript-eslint/eslint-plugin": "^6.0.0",
    "@typescript-eslint/parser": "^6.0.0",
    "eslint": "^8.0.0",
    "ts-node": "^10.0.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@types/marked": "^5.0.2",
    "chalk": "^4.1.2",
    "commander": "^11.0.0",
    "marked": "^15.0.12",
    "marked-terminal": "^7.3.0"
  }
}
